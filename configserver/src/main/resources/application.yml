
spring:
  application:
    name: config-server
  profile:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhista-111005
        native:
          search-locations: classpath:/config
server:
  port: 8888